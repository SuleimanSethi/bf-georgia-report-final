import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';

const inter = Inter({
  subsets: ['latin'],
  weight: ['300', '400', '500', '600', '700'],
  display: 'swap',
});

export const metadata: Metadata = {
  title: 'Georgia Collection — Campaign Analytics | Brawn & Fox',
  description:
    'Campaign performance analytics for the Brawn & Fox Georgia Collection. March 24 – April 13, 2026. 15-day integrated Meta Ads + Klaviyo campaign.',
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en" className={inter.className}>
      <body>{children}</body>
    </html>
  );
}
